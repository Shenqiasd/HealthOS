# Observability Contracts

Shared correlation IDs and provider-neutral structured telemetry contracts. The package exposes a deep-redacting event factory and a bounded in-memory sink for tests; it intentionally contains no network exporter, endpoint configuration, or credentials.
