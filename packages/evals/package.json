{
  "name": "@healthos/evals",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate:fixtures": "tsx src/generate-rule-fixtures.ts",
    "eval:rules": "tsx src/run-rule-cases.ts",
    "eval:coach": "./bin/eval-coach",
    "eval:food": "TSX_TSCONFIG_PATH=../../tsconfig.base.json tsx src/run-food-evals.ts",
    "test:coach-isolation": "node --test src/coach-isolation-contract.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@healthos/contracts": "workspace:*",
    "@healthos/rules": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.20.0",
    "tsx": "4.21.0",
    "typescript": "5.9.3"
  }
}
